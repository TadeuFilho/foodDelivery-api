name: $(BUILD.BUILDID)

variables:
- template: build-variables.yml
- group: RLOG-TRANSPORT-ORDER

resources:
  repositories:
    - repository: devops-ci
      type: git
      name: devops-ci
      
stages:
  - template: azure-pipelines-ms-template.yml@devops-ci
