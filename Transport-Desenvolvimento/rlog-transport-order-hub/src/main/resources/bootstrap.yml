spring:
  application:
    name: rlog-transport-order-hub
  cloud:
    config:
      #uri: ${SPRING_CONFIG_URI}
      uri: ${SPRING_CONFIG_URI:http://10.210.12.250/oms/config-server}
      username: ${SPRING_CONFIG_USERNAME:omsuser}
      password: ${SPRING_CONFIG_PASSWORD:Sup3rS3cr3tPassword0}

health:
  config:
    enabled: false

app:
  build:
    version: v1
    domain: rlog
renner:
  library:
    mongo:
      disable: true